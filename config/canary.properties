# This is the configuration file for Canary.
# This file exists for debugging purposes and should not be configured otherwise.
# Before configuring anything, take a backup of the worlds that will be opened.
#
# You can find information on editing this file and all the available options here:
# https://github.com/AbdElAziz333/Canary/wiki/Configuration-File
#
# By default, this file will be empty except for this notice.
mixin.world.chunk_tickets=false
# Disabled 2026-05-19: Canary's ai.poi.PoiManagerMixin merges getInRange at priority 1000,
# eliminating the INVOKE target Valkyrien Skies' MixinPOIManager @WrapOperation needs.
# Without this toggle the integrated server crashes during ServerLevel.<init> at boot.
# Cost: lose Canary's POI/villager scan optimizations. Restore if VS suite is removed.
mixin.ai.poi=false
